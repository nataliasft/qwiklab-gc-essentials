sudo su - #get root access
apt-get update #update OS
apt-get install nginx -y #install NGINX, -y meanes "yes" as answer to all prompts
ps auwx | grep nginx #confirm nginx running
#a = show processes for all users, u = display the process's user/owner, x = also show processes not attached to a terminal
#to see webpage click the External IP link in the row





